{
  "name": "bunny",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "yarn build && node ./build/index.js",
    "build" : "babel src -d build",
    "build:docker": "docker build -f Dockerfile -t virtual-browser .",
    "dev": " yarn build:docker && docker run --rm -it --net=host --shm-size=\"1gb\" virtual-browser -s \"wss://192.168.0.12:443\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "argv-parse": "^1.0.1",
    "commander": "^3.0.1",
    "fluent-ffmpeg": "^2.1.2",
    "simple-peer": "^9.5.0",
    "wrtc": "^0.4.1",
    "ws": "^7.1.2"
  },
  "devDependencies": {
    "@babel/cli": "^7.6.0",
    "@babel/core": "^7.6.0",
    "@babel/preset-env": "^7.6.0"
  },
  "resolutions": {
    "minipass": "2.7.0"
  }
}
